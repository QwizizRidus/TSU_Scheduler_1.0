<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TSUScheduler" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimaryVariant">@color/teal_700</item>
        <item name="colorPrimary">@color/teal_200</item>
        <!-- Secondary brand color. -->

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- Customize your theme here. -->
        <item name="toolbarColor">@color/teal_200</item>
        <item name="navDrawerColor">@color/white</item>
        <item name="scheduleHeaderColor">@color/black</item>
    </style>
</resources>